# AHUT 晚寝签到修复审查

## 当前结论

已基于远程默认分支提交 `d834c6a2485266e37d6f37af09a55f90e81d5ec9` 检查代码并完成本地修复。尚未推送到 GitHub、未创建 PR，也未使用真实凭据连接学校。

## 本次读取到的线上证据

- 默认分支为 `main`，`AHUT nightly 21:35` 工作流处于 `active` 状态。
- [运行 35447048224](https://github.com/issacrudnic322-ctrl/1/actions/runs/35447048224) 和 [运行 35243313593](https://github.com/issacrudnic322-ctrl/1/actions/runs/35243313593) 都是 `workflow_dispatch`。公开的作业步骤记录显示 `Check configuration` 成功、`Run nightly sign-in` 被跳过。
- 查询运行列表时总数为 2，没有 `schedule` 事件。没有足够证据判断为何定时触发未出现。
- 本次下载完整作业日志返回 HTTP 403。上述结论来自公开的步骤记录，不是本次下载的完整日志。
- 本次 GitHub 连接接口返回 `Unknown tool`；Git 推送预检提示没有可用的认证用户名/凭据。无法推送分支、创建 PR 或触发实际运行；不能据此确定用户账号侧具体授予了哪些权限。

## 修改内容

1. 手动运行默认改为 `sign-in`；仅检查配置仍可通过明确选择 `check` 执行。运行标题和作业名区分两者。
2. 修正 `generate_signCode` 的秒/毫秒不匹配：函数使用毫秒时间戳，调用方原来传入秒，导致参与签名的日期错误。此问题由本地代码和回归测试确认，但未证明它是学校实际拒绝签到的唯一原因。
3. 获取宿舍信息失败或数据不完整时停留在失败步骤，不继续发送签到请求。
4. 登录、任务及宿舍响应必须通过 HTTP 与业务状态检查；非对象 JSON 和空 Token 不作有效结果。
5. 显式传播 `api_confirmed` / `already_signed`，仅到达最后一步或返回含糊的 `success` 不能让入口报告成功。
6. 摘要只写入本地定义的状态、阶段和时间，既不包含凭据，也不包含学校原始响应。接口确认与独立查询当日记录分开说明。
7. 增加不读取 Secrets 的离线回归测试工作流。

## 验证结果

| 验证对象 | 结果 |
| --- | --- |
| 13 项离线回归测试 | Python 3.12.14 下全部通过，未使用真实凭据、未请求学校接口；Actions 的 Python 3.13 验证仍待运行 |
| 工作流 YAML | 解析通过 |
| cron 时区 | `35 13 * * *` 的 UTC 13:35 对应北京时间 21:35 |
| 手动模式默认值 | 已验证为 `sign-in` |
| 凭据与原始错误正文输出 | 使用模拟敏感字符串测试，未出现在入口日志或摘要 |
| 差异空白检查 | `git diff --check` 通过 |
| 实际 GitHub 定时事件 | 未验证；原工作流虽启用，查询时无定时记录 |
| 真实学校登录及签到 | 未执行、未验证 |
| GitHub 提交 / PR | 尚未完成，当前环境没有可用的认证写入通道 |

## 后续验证

在具备该仓库访问权限的环境中应用附带补丁，执行 `python -m unittest discover -s tests -v`，推送修复分支并审查差异。需要能够修改仓库代码及工作流、创建 PR 和运行 Actions 的认证连接；无需在聊天中提供 Secrets 的值。

合并到默认分支后，在北京时间 21:35 之后手动运行 `sign-in`。确认摘要是学校接口确认状态，再在学校小程序核对当日记录。下一次计划时刻后，单独核查新的运行是否由 `schedule` 触发。两项都完成才能分别确认实际签到和定时触发。

GitHub 的计划任务可能延迟甚至在高负载时丢弃排队任务，不能保证精确到 21:35。当前 cron 本身正确，本补丁不宣称已解决服务端未触发的原因。[GitHub 官方说明](https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows#schedule)
